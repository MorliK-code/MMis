use_default_settings: true

general:
  debug: false
  instance_name: "MMis SearxNG"

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "mmis-searxng-secret-change-me"
  limiter: false
  public_instance: false

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "all"
  formats:
    - html
    - json

outgoing:
  request_timeout: 8.0

engines:
  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg
